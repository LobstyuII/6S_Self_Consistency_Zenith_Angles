python main.py --phase all --band band1 --model_type lut


$bands = @("band1", "band2", "band3", "band4", "band5", "band6")
foreach ($band in $bands) {
python main.py --phase simulate --band $band
python main.py --phase analyze --band $band
}


$bands = @("band1", "band2", "band3", "band4", "band5", "band6")
foreach ($band in $bands) {
python main.py --phase all --band $band
}


python main.py --phase all --mode full --band all


python main.py --phase analyze --band band1

python recreate_data.py

# 方式1：使用现有数据生成论文图表
python main.py --phase paper_figures --mode full --band all

# 方式2：先生成数据，再生成论文图表
python main.py --phase simulate --mode paper_figures --band all
python main.py --phase paper_figures --mode full --band all --paper_figures_only

# 方式3：单独运行论文图表生成器
python paper_figures.py


# 1. 生成任务块
python main.py --phase block_simulate --block_mode generate --band band3

# 2. 运行模拟（从头开始）
python main.py --phase block_simulate --block_mode run --band band6 --max_blocks 50

# 3. 恢复模拟（继续运行）
python main.py --phase block_simulate --block_mode resume --band band6

# 4. 合并数据
python main.py --phase block_simulate --block_mode merge --band band3

# 5. 选择性合并（如只要sza<30的数据）
python main.py --phase block_simulate --block_mode merge --band band3 --merge_constraints "sza:0-30"


# 快速测试 - 只处理band3的1000个组合
python main.py --block_mode run_parallel --band band3 --n_workers 4

# 全量处理 - 所有波段
python main.py --block_mode run_parallel --band all --n_workers 8

# 指定波段和处理数量
python main.py --block_mode run_parallel --band band6 --n_workers 6


python batch_parallel.py --band band1 --chunk_size 100 --n_workers 2

python batch_parallel.py --band band6 --chunk_size 100 --n_workers 8

