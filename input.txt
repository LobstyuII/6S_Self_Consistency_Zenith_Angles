# 生成训练数据（蒙特卡洛采样）
python main_workflow.py --training_samples 5000 --bands band3 --n_workers 8

# 生成验证网格（稀疏规则网格）
python main_workflow.py --validation_grid --bands band3 --n_workers 8

# 训练ML模型
python main_workflow.py --train_ml --ml_models RandomForest,XGBoost --ml_sample_ratio 0.5

# 生成业务化LUT
python main_workflow.py --generate_lut --ml_model_path ./models/best_model.pkl